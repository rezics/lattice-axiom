{
  "$schema": "../../meta/schemas/requirements.schema.json",
  "schema_version": 1,
  "owner": "platform:rendering",
  "requirements": [
    {
      "id": "RENDERING-001",
      "document_id": "platform.rendering.rendering",
      "level": "must",
      "statement": "The Bevy render host consumes locked render capabilities and bounded derived meshes without introducing a parallel renderer or authoritative effects.",
      "acceptance": [
        "Six-face, halo, material grouping, stale work and device-loss gates pass.",
        "Headless omission does not change authoritative registration or world hash."
      ]
    }
  ]
}
