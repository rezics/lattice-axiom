{
  "$schema": "../../meta/schemas/requirements.schema.json",
  "schema_version": 1,
  "owner": "platform:assets",
  "requirements": [
    {
      "id": "ASSET-SEMANTICS-001",
      "document_id": "platform.assets.asset-semantics",
      "level": "must",
      "statement": "Locked content resolves versioned asset semantics and deterministic fallbacks without changing authoritative identity.",
      "acceptance": [
        "All required asset references close before activation.",
        "Headless omission and presentation fallback leave authoritative hashes unchanged."
      ]
    }
  ]
}
