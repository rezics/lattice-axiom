{
  "$schema": "../../meta/schemas/requirements.schema.json",
  "schema_version": 1,
  "owner": "platform:compatibility",
  "requirements": [
    {
      "id": "COMPATIBILITY-001",
      "document_id": "platform.compatibility.versioning-and-compatibility",
      "level": "must",
      "statement": "Package, registration, wire, save and engine-coupled compatibility are governed by their explicit version coordinates rather than a global switch.",
      "acceptance": [
        "Compatible and incompatible reopen cases produce deterministic diagnostics.",
        "Upgrade rehearsal preserves all declared old data contracts."
      ]
    }
  ]
}
