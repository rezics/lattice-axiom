{
  "$schema": "../../../meta/schemas/requirements.schema.json",
  "schema_version": 1,
  "owner": "@latticeaxiom/progress",
  "requirements": [
    {
      "id": "PROGRESS-GRAPH-001",
      "document_id": "package.latticeaxiom.progress.index",
      "level": "must",
      "statement": "A deterministic persistent progress graph accepts package-contributed chapters without host-owned content order.",
      "acceptance": [
        "Cycle diagnostics and graph hash are stable under registration order.",
        "Progress survives save and reopen through stable identities."
      ]
    }
  ]
}
