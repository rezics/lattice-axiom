{
  "$schema": "../../../meta/schemas/requirements.schema.json",
  "schema_version": 1,
  "owner": "terrenia",
  "requirements": [
    {
      "id": "TERRENIA-ROOT-001",
      "document_id": "package.terrenia.main.index",
      "level": "must",
      "statement": "The Terrenia root selects a replaceable dimension closure and owns only dimension-level grants and semantic bindings.",
      "acceptance": [
        "A different root dimension starts without host code changes.",
        "Terrenia-specific IDs are absent from platform fallback paths."
      ]
    }
  ]
}
