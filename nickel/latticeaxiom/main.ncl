# Canonical entry for the versioned latticeaxiom.lib v2 source root.
{
  common = import "common.ncl",
  game = import "game.ncl",
  package = import "package.ncl",
  registration = import "registration.ncl",
}