# latticeaxiom-storage-rocksdb (milestone 3)

Placeholder. Will be the production `WorldStorage` implementation on RocksDB
(ADR 0009) and the only crate allowed to depend on `rocksdb` (enforced by
`deny.toml`). Must pass the same storage conformance and kill-test suites as
the in-memory implementation.
