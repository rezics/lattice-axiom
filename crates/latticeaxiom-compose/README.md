# latticeaxiom-compose (milestone 2)

Placeholder. Will embed the Nickel evaluator (`nickel-lang`) as the single
evaluation boundary (ADR 0007/0010): evaluate `package.ncl` / `game.ncl` with
the versioned `latticeaxiom.lib` contracts and convert the result directly
into the strongly typed `CompositionSpec` — no intermediate JSON.
