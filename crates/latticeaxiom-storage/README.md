# latticeaxiom-storage (milestone 3)

Placeholder. Will define the `WorldStorage` contract (ADR 0009) with the
in-memory reference implementation and the storage conformance suite,
mirroring how `latticeaxiom-render` fronts its backends. Signed coordinates in
persistent keys use sign-bit-flipped big-endian encoding (see AGENTS.md).
