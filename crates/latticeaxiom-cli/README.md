# latticeaxiom-cli (milestone 2)

Placeholder. Will provide the first CLI: `check`, `lock`, `build`, `pack`,
and `doctor` (see the package-management architecture page). The package name
follows ADR 0012; the executable may get a short alias later.
