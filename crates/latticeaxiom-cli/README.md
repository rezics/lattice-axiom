# latticeaxiom-cli

Milestone 2 command line for `check`, `lock`, `build`, `pack`, and `doctor`.
All commands share the embedded evaluator and typed package kernel; none invokes
an external Nickel process or uses JSON as an internal semantic bus.
