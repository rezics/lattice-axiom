[package]
name = "latticeaxiom-voxel-mesh"
description = "In-house voxel meshing: visible-face culling and greedy merging, native to the Z-up world convention."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
repository.workspace = true

[dependencies]
# Deliberately none: this crate is pure data-to-data and must not depend on
# the renderer, the world model, or any backend.

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
