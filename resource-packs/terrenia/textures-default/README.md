# `@terrenia/textures-default`

Independent client-only textures pack. Select it in the client resources profile.
Its data artifact is locked separately from the authoritative world.
Only textual material definitions and shader code are committed.
