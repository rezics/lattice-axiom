schema_version = 1
name = "@latticeaxiom/webview"
version = "0.1.0"
domains = ["client"]
trust = "trusted-native"

[realizations.native-static]
id = "native-static"
kind = "native-static"
domains = ["client"]
artifact = { kind = "source-build" }
trust = "trusted-native"

[nickel_public_entrypoints]
default = "package.ncl"

[source_inclusion]
include = ["latticeaxiom-package.toml", "package.ncl", "crates", "README.md"]

[rust]
entry = "crates/latticeaxiom-webview/Cargo.toml"
members = ["crates/latticeaxiom-webview/Cargo.toml"]
