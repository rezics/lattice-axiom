schema_version = 1
name = "@latticeaxiom/progress"
version = "0.1.0"
domains = ["authoritative", "client"]
trust = "data-only"

[provides."latticeaxiom:capability/progress-graph@1"]
capability = "latticeaxiom:capability/progress-graph@1"
version = "1.0.0"
cardinality = "exactly-one"
domains = ["authoritative", "client"]

[realizations.data]
id = "data"
kind = "data"
domains = ["authoritative", "client"]
artifact = { kind = "data-root", path = "data" }
trust = "data-only"

[nickel_public_entrypoints]
default = "package.ncl"

[source_inclusion]
include = ["package.ncl", "latticeaxiom-package.toml", "data"]
