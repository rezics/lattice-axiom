schema_version = 1
name = "@latticeaxiom/inspect"
version = "0.1.0"
domains = ["client"]
trust = "data-only"

[dependencies.observability]
package = "@latticeaxiom/observability"
version = "~0.1.0"
optional = false
domains = ["client"]

[requires."latticeaxiom:capability/diagnostic-registry@1"]
capability = "latticeaxiom:capability/diagnostic-registry@1"
version = "^1.0.0"
provider = "@latticeaxiom/observability"
cardinality = "exactly-one"
domains = ["client"]

[provides."latticeaxiom:capability/target-inspect-surface@1"]
capability = "latticeaxiom:capability/target-inspect-surface@1"
version = "1.0.0"
cardinality = "exactly-one"
domains = ["client"]

[realizations.data]
id = "data"
kind = "data"
domains = ["client"]
artifact = { kind = "data-root", path = "data" }
trust = "data-only"

[nickel_public_entrypoints]
default = "package.ncl"

[source_inclusion]
include = ["data", "package.ncl", "latticeaxiom-package.toml"]
