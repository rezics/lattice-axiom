schema_version = 1
name = "@latticeaxiom/dev-tools"
version = "0.1.0"
domains = ["client", "tool"]
trust = "data-only"

[dependencies.settings]
package = "@latticeaxiom/settings"
version = "~0.1.0"
optional = false
domains = ["client", "tool"]

[dependencies.observability]
package = "@latticeaxiom/observability"
version = "~0.1.0"
optional = false
domains = ["client", "tool"]

[requires."latticeaxiom:capability/settings-registry@1"]
capability = "latticeaxiom:capability/settings-registry@1"
version = "^1.0.0"
provider = "@latticeaxiom/settings"
cardinality = "exactly-one"
domains = ["client", "tool"]

[requires."latticeaxiom:capability/diagnostic-registry@1"]
capability = "latticeaxiom:capability/diagnostic-registry@1"
version = "^1.0.0"
provider = "@latticeaxiom/observability"
cardinality = "exactly-one"
domains = ["client", "tool"]

[provides."latticeaxiom:capability/debug-workbench@1"]
capability = "latticeaxiom:capability/debug-workbench@1"
version = "1.0.0"
cardinality = "exactly-one"
domains = ["client", "tool"]

[realizations.data]
id = "data"
kind = "data"
domains = ["client", "tool"]
artifact = { kind = "data-root", path = "data" }
trust = "data-only"

[nickel_public_entrypoints]
default = "package.ncl"

[source_inclusion]
include = ["package.ncl", "latticeaxiom-package.toml"]
